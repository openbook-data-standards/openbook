# Retired names (Q35)

Once a frozen id, list-value, or field name is shipped it is never reused.
This file is the registry. Empty until something is retired.

Machine-readable: [`deprecated.json`](deprecated.json).
