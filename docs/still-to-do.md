# Still to do

Phase B on this branch is recorded through **Q45**. Next decision is **Q46**.
Do not implement Q46 until it is asked and answered.

## Next question

- **Q46 — Q33 names.** Level L already MUST (Q33). The wire names are not
  decided: caught-up marker, heartbeat, stale `since` (pull too-old). Leave
  this as the next question.

## Later (named in decisions, not decided)

- **Q41 shapes** — `ttl` field name; discovery document JSON.
- **Q33 remainder** — honest-conflation flag (item 6) still unnamed.
- **Q38** — CI that flags doc names that are not on a schema.
- **Q37** — consumer-view schema (ignore unknown fields).
- **Q42 deferred** — CloudEvents wrap; DNS-style id namespace.
- **Pull OpenAPI** — building-blocks mention OpenAPI 3.1 for `since=`; no
  document in-repo yet.

## Done on this walk (Q32–Q45)

Q32 FULL-TRANSITIVE · Q33 eight MUST (prose) · Q34 unknown/other · Q35 never
reuse · Q36 deprecation window · Q37 closed write / open read · Q38 camelCase
· Q39 decimal strings · Q40 corpus · Q41 GBFS principles · Q42 AsyncAPI ·
Q43 takedown = marketStatus + tombstone · Q44 `baseCurrency` · Q45 market
`limit` / most-specific-wins.
