# Still to do

Questions recorded through **Q88**. Catalog pass closed and on the wire.
**Q88** `throws` / `bats` are on `player`. Q46–Q49 are on the wire. **Q50**
is the docs-vs-schema name check in `tools/validate.py`.

## Later PRs (decided, not built)

- **Q55** — schema-diff CI for frozen majors only (1.0+).
- **Q80** — match lineup live object (paused: roster player ids; object
  name lineup as titled).
- Racing draw/stall, cricket toss, playoff series state — not on generic
  fixture (**Q86**); later stage / sport slice.

## Next question (not decided)

- (none parked from this walk)

## Done (Q32–Q88)

Q32–Q55 · **Q11** names · catalog place/league/`gender`/`ageGroup`/`surface`/`seed` · **Q88** `throws`/`bats`.
